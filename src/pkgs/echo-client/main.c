#include <mu-api/bootstrap.h>
#include <mu-base/std.h>

int mu_main(unused MuArgs args)
{
    // cstr msg = strdup("Hello, World !");
    // MuCap echo = bootstrap_lookup("engineering.cute.echo");

    // MuMsg data = mu_msg(0, msg, NULL, true);

    // mu_ipc(&echo, &data, MU_IPC_SEND);

    return 0;
}